ARG NODE_VERSION=20-alpine
FROM node:${NODE_VERSION}

WORKDIR /app

COPY package*.json ./
RUN npm ci --omit=dev

COPY . .

ENV NODE_ENV=production
ENV PORT=4006

EXPOSE 4006

CMD ["node", "services/prescriptions.js"]
